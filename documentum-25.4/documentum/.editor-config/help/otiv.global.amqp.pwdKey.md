### Global AMQP Password Key

<!-- Variable Description -->
Key for the RabbitMQ broker password. Defaults to rabbitmq-password. 

#### Variable Location
```yaml
# documentum/config/passwords.yaml or passwords_vault.yaml or documentum/config/passwords_k8api.yaml
otiv:
  global:
    amqp:
      pwdKey: 
```