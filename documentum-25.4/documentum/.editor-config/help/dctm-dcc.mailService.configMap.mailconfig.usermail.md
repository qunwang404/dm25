### User Mail ID

<!-- Variable Description -->
User mail if mail server requires user credentials.

#### Variable Location
```yaml
# documentum/config/configuration.yml
mailService:
	configMap:
     mailconfig:
		usermail:
```