### Java Management Extensions Password

<!-- Variable Description -->
Java Management Extensions (JMX) server password.

#### Variable Location
```yaml
# documentum/config/passwords.yaml or passwords_vault.yaml or documentum/config/passwords_k8api.yaml
dctm-dms:
  jmx:
    password:  
```