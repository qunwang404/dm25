### REST Store BaseURL

<!-- Variable Description -->
URL that Documentum CM Server uses to communicate with the REST store.

#### Variable Location
```yaml
## documentum/config/passwords.yaml OR documentum/config/passwords_vault.yaml or documentum/config/passwords_k8api.yaml
cs-secrets:
  restStore:
  restStoreBaseUrl:
```