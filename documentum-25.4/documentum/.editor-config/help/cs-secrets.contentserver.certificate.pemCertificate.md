### Private Certificate

<!-- Variable Description -->
Certificate in the PEM format. Make sure that you change the default value and is consistent.

#### Variable Location
```yaml
# documentum/config/passwords.yaml or passwords_vault.yaml or documentum/config/passwords_k8api.yaml
content-server:
  contentserver:
    certificate:
      pemCertificate: 
```