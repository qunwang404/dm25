# D2Config App Title

Configure the application title in the D2Config project. It is used to set and manage the title displayed in the application interface.
By Default the appTitle is "D2-Config" that gets displayed on the Browser tab.
Update to a custom appTitle below when required

```yaml
d2config:
  appTitle: D2-Config
```