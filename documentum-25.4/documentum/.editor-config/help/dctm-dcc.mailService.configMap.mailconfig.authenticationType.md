### Auth Type

<!-- Variable Description -->
Authentication type. The valid values are basic or oauth.

#### Variable Location
```yaml
# documentum/config/configuration.yml
mailService:
	configMap:
     mailconfig:
		authenticationType: oauth
```