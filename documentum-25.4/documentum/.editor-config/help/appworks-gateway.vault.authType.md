### Vault Auth Type

<!-- Variable Description -->
Format that is used to authenticate the pods. For example, "jwt" for JSON web token.

#### Variable Location
```yaml
# documentum/config/configuration.yml
appworks-gateway:
  vault:
    authType:
```