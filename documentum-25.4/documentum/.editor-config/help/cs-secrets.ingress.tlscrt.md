### TLS Certificate

<!-- Variable Description -->
Base64-encoded TLS certificate in one line without any carriage returns or line wrap.

#### Variable Location
```yaml
# documentum/config/passwords.yaml or passwords_vault.yaml or documentum/config/passwords_k8api.yaml
cs-secrets:
  ingress:
    tlscrt:  
```