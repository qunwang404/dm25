### Root Password

<!-- Variable Description -->
Password of the installation root directory.

#### Variable Location
```yaml
# # documentum/config/passwords.yaml OR documentum/config/passwords_vault.yaml or documentum/config/passwords_k8api.yaml
cs-secrets:
  contentserver:
    install:
      root:
        password:
```