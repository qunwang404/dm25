### SMTP Server

<!-- Variable Description -->
SMTP host name or IP address accessible to both the Documentum CM Server and client pods.

#### Variable Location
```yaml
# documentum/config/addons/lss/ls-config.yaml
smtpServer: &smtp_server <SMTP_SERVER>
```