### Vault Proxy Address

<!-- Variable Description -->
Proxy address through which the vault information is routed if you are using the Vault Agent Injector.

#### Variable Location
```yaml
# documentum/config/configuration.yml
otds:
  otdsws:
    vault:
      proxyAddress:
```