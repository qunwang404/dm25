### SMTP Port

<!-- Variable Description -->
SMTP port accessible to both the Documentum CM Server and client pods.

#### Variable Location
```yaml
# documentum/config/addons/lss/ls-config.yaml
smtpPort: &smtp_port <SMTP_PORT>
```