### Custom Secret Name

<!-- Variable Description -->
Name of an existing secret object. This is useful when OTDS charts work as subcharts and parent chart creates a single secret.

#### Variable Location
```yaml
# documentum/config/configuration.yml
otds:
  otdsws:
    customSecretName:
```